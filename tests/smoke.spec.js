const {test,expect}=require('@playwright/test');
test('game starts, drag-aims, fires, and exposes FX hooks',async({page})=>{const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await page.goto('http://127.0.0.1:8000');await page.waitForFunction(()=>window.__DEBUG?.state==='READY');expect(await page.locator('canvas').screenshot()).toBeTruthy();await page.locator('#start').click();const canvas=page.locator('canvas'),box=await canvas.boundingBox();await page.mouse.move(box.x+box.width*.5,box.y+box.height*.65);await page.mouse.down();await page.mouse.move(box.x+box.width*.72,box.y+box.height*.42,{steps:12});await page.mouse.up();expect(await page.evaluate(()=>__DEBUG.angle)).toBeGreaterThan(0);await page.keyboard.press('Space');expect((await page.evaluate(()=>__DEBUG.entityCounts())).bullets).toBeGreaterThan(0);await page.evaluate(()=>__DEBUG.forceHit());await page.screenshot({path:'tests/hit.png'});await page.evaluate(()=>__DEBUG.forceMiss());expect(errors).toEqual([])});
